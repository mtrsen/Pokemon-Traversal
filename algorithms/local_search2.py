#!/usr/bin/env python
import networkx as nx
import math

#todo: make a dict of the path, which contains the cost, ...
#todo: examine the return of the move function

#
# def make_move():

#def check_cost():

def local_search2(G, cutoff, seed):
    """
    Local search - 2
    :param G:
    :param cutoff:
    :param seed:
    :return:
    """
    """
    global best_cost, best_path
    global T
    for i in make_move(path):
        path i.cost = check_cost(path i)
        if(path i.cost < current best OR (e**(-1*delta)/(T*1.0)):
            best_cost = path i.cost
            best_path = path i
        temp = T*0.95
        """





    pass

if __name__ == '__main__':
    print('Run the main.py file')
